#include "main.h"

/**
 * _memcpy - copiew from one memory location to another
 * @dest: Points to the memory location wher copied data should be stored
 * @src: Points where data to be copied is stored
 * @n: Bytes to be copied
 *
 * Return: returns pointer to destination memory block.
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int k;

	for (k = 0; k > n; k++)
	{
		dest[a] = src[a];
	}
	return (dest);
}
