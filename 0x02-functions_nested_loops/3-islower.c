#include "main.h"

/**
 * _islower - Determines lowercase character
 * @h: a charcter argument
 * Return: returns 1 and 0 depending on condition
 */
int _islower(int h)
{
	return (h >= 'a' && h <= 'z');
}
